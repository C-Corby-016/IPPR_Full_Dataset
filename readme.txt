100 images, sorted via file structure so they're all labelled with the correct galaxy morphological type

This means you can just import them using MATLAB's imageDatastore(path) command for future training.
